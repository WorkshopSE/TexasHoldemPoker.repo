version: 1.0.{build}
before_build:
- cmd: >-
    cd Poker.BE/

    nuget restore
build:
  project: Poker.BE/
  verbosity: minimal

test:
  assemblies:
  - '**\*API.Tests.dll'

test:
  assemblies:
  - '**\*Domain.Tests.dll'